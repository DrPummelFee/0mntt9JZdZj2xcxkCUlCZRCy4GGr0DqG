<?php

session_start();

if(isset($_SESSION['userid']))
{
echo '<a href="logout.php">Logout</a>';
if(isset($_POST['send']))
{
    $user = $_POST['user'];
    $pass = $_POST['pass'];
    
    $username = '';
    $password = '';
    
    if($user == $username && $pass == $password)
    {
        $_SESSION['userid'] = 1;
    }
    else
    {
        $error = true;
    }
}
